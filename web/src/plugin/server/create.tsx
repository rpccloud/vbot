import React from "react";

interface ServerCreateProps {
    param: string,
}

const ServerCreate = (props: ServerCreateProps) => {
    return (
        <div>
            Plugin server.create
        </div>
    )
}

export default ServerCreate
